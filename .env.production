# Production build config. These are PUBLIC values by design:
#  - the Supabase project URL is the public API endpoint
#  - the publishable (anon) key is meant to ship in the browser bundle and is
#    protected by Row Level Security
# Committing them makes every build (Cloudflare, GitHub Pages, local) work
# without per-host env-var configuration. Do NOT put the service_role key here.
VITE_SUPABASE_URL=https://ybpadmmooftyptagvnje.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_n4Hz_JmFGP8ar_rI8UvDvg_svUgQAv-
